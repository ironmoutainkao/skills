# 自定义指令（Custom Commands）

自定义指令功能允许您将常用的提示词（Prompts）和工作流封装为可复用的命令。只需在 Agent 对话框中输入 `/`，即可快速调用这些指令，显著提升日常开发效率。

## 指令类型与作用范围

| 特性 | 用户级指令 | 项目级指令 |
| --- | --- | --- |
| **作用范围** | 对当前用户的所有项目生效 | 仅在当前项目根目录及其子目录中生效 |
| **存储路径** | `~/.qoder/commands/` | `<项目根目录>/.qoder/commands/` |
| **适用场景** | 通用开发任务，如审查代码、生成单元测试 | 项目专属任务，如检查本项目 API 规范 |
| **共享方式** | 仅限当前用户使用 | 可通过 Git 等版本控制系统与团队成员共享 |

> **注意**：用户级指令不支持跨设备同步，您可以手动迁移配置文件。

## 创建自定义指令

1. **打开指令管理界面**
   - 方法一：进入 Qoder 设置中的指令页面，点击 **添加** 按钮
   - 方法二：在对话框中输入 `/`，点击底部的 **添加指令** 快捷入口

2. 在顶部搜索栏中输入一个**唯一指令名称**（如 `gen-test`），然后按 **回车**

3. 选择指令类型：
   - **用户级**：适用于所有项目的通用命令
   - **项目级**：仅在当前项目中可用

4. 在引导文案区域填写完整的提示词内容，保存文件后将自动保存您的指令

5. 返回会话，在对话框内输入 `/` 即可看到新创建的指令

## 示例指令

### /code-inspect

用于评估源代码的系统化框架，包含：
- 技术实现检查
- 开发标准验证
- 风险管理
- 维护性评估

### /security-check

系统安全态势的系统化检查：
- 应用安全
- 认证系统
- 数据保护
- 系统架构

### /create-pr

用于提交代码更改的标准化流程：
- 代码质量保证
- 变更文档
- 测试验证

### /project-test

执行项目测试套件：
- 环境设置
- 测试执行
- 问题解决
